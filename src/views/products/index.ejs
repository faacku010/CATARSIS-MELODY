<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catarsis Melody</title>
    <link href="/css/index.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/d6458ce523.js" crossorigin="anonymous"></script>
</head>

<body>
  
<%- include('../partials/header.ejs') %>

<main>
    <h2>BUSCAR POR CATEGORIAS</h2>

    <section class="destacados">
        <% productos.forEach(productos => { %>
            <article>
                <a href="/productos/detalle/<%= productos.id %>">
                    <img src="/images/products/<%= productos.imagen_producto %>" class="image-destacado">
                </a>
                    <p><%= productos.nombre %></p>
                    <p>$ <%= productos.precio %></p>                
            </article>
        <% }) %>
         </section>   

                
    <h2>DESTACADOS</h2>   
    
    <section>
        <% for( let i = 0; i <= 5; i++ ) { %>
            <article>
                <img src="/images/<%= productos[i].imagen_producto %>" class="image-destacado">
                <p><%= productos[i].nombre%> - <%= productos[i].categorias.nombre %>
                </p>
                <p>$ <%= productos[i].precio %></p>                
            </article>
        <% } %>
    </section>

</main>  

<%- include('../partials/footer.ejs') %>

</body>
